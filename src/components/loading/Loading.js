import React from 'react'

/**
 * Component to show loading screen.
 */
function Loading() {
    // Returning loading message. We can change this to proper loading screen later.
    return (
        <div>Loading...</div>
    )
}

export default Loading